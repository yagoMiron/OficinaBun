{
  "name": "index",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --watch index.ts",
    "test": "bun test --watch --coverage",
    "socket": "bun --watch src/websocket.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}